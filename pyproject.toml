[project]
name = "vortezwohl"
version = "0.0.17"
description = "Practical SDKs by vortezwohl."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "overrides",
    "psutil",
    "typing-extensions",
]
