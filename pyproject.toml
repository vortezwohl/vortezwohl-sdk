[project]
name = "vortezwohl"
version = "0.0.16"
description = "SDK of vortezwohl."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "overrides",
    "psutil",
    "typing-extensions",
]
